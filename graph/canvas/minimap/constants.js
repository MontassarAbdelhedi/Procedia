/**
 * @fileoverview Constants for the minimap module.
 * @dependencies none
 */

(function(m) {
  m.C = {
    MINIMAP_W:    160,
    MINIMAP_H:    100,
    NODE_WIDTH:   180,
    NODE_HEIGHT:  120,
    DOT_RADIUS:   3,
    MAX_VIEW_FRAC: 0.5
  };
})(window.__minimap = window.__minimap || {});
