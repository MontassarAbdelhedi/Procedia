-----------
add a x button to effector node: remove the node and rewire upstream to downstream.
example:
text node -> fill node -> comp node
deleting fill node will auto-wire: text node -> comp node
-----------