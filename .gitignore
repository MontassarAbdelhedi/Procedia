# OS
Thumbs.db
.DS_Store
*.swp
*.swo

# Editor
.idea/
.vscode/
*.sublime-*

# CEP / ExtendScript
*.log
*.tmp
*.bak

# Node / dependencies
node_modules/
package-lock.json
yarn.lock

# Debug / local config
.debug
settings.local.json
.claude/
.opencode/

# Secrets (Sentry DSN / reporting API — never commit)
.secrets/

# Release artifacts
dist/
latest.json

# Build output (secrets injected at build time — never commit)
build/
