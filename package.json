{
  "name": "procedia",
  "private": true,
  "version": "0.0.4",
  "description": "Procedia — visual node-based compositing for After Effects",
  "author": "Uppercut Studio",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "version:sync": "node scripts/sync-version.js",
    "build": "npm run version:sync && node scripts/build.js",
    "release": "npm run version:sync && npm test && node scripts/build.js && node scripts/package-release.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^3.0.0"
  }
}
